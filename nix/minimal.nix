# gnunix-minimal ships a working Nix and nothing else on top of the LFS base
# (ADR-003), so this profile is empty. It exists so the image has a system
# profile to --set, and a place for packages to land when it grows one.
pkgs:

[ ]
